# MCA App Frontend — Environment Variables
# Dev: use /api (Vite dev server proxies to http://localhost:5000)
VITE_API_BASE=/api

# Production: point to your deployed backend
# VITE_API_BASE=https://your-api.example.com/api
