# MCA App Frontend — API base URL
# Dev (recommended): /api — Vite proxies to localhost:5000 (desktop + mobile on same Wi-Fi)
# Prod: set to your deployed API, e.g. https://api.example.com/api
VITE_API_BASE=/api
